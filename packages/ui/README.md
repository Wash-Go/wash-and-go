# @wash-and-go/ui
Design tokens + renderer-specific component sets: `src/tokens` (shared), `src/web` (Tailwind), `src/native` (RN). Two component libraries sharing tokens — NOT one cross-platform library (debate D-13.1).
